# Copy to terraform.tfvars and fill in with real values before `terraform apply`.

aws_region  = "us-east-1"
aws_profile = "default"

vpc_id              = "vpc-xxxxxxxxxxxxxxxxx"
subnet_ids          = ["subnet-xxxxxxxxxxxxxxxxx", "subnet-yyyyyyyyyyyyyyyyy"]
security_group_ids  = ["sg-xxxxxxxxxxxxxxxxx"]

model_data_url            = "s3://my-bucket/iris/model.tar.gz"
model_artifact_bucket_arn = "arn:aws:s3:::my-bucket"

# Build and push ../container/ to the aws_ecr_repository.iris_ic repo this
# stack creates before applying - see variables.tf for the exact commands.
container_image_tag = "latest"

tags = {
  project = "iris-ic"
}

# Optional. Leave kms_key_id unset to use the KMS key this stack creates
# (kms.tf) instead of an existing one.
# kms_key_id = "arn:aws:kms:us-east-1:123456789012:key/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# Data capture is NOT supported on endpoints with Inference Components
# enabled - confirmed against a live account 2026-08-26. AWS rejects
# CreateEndpointConfig with a DataCaptureConfig for these endpoints outright.
# Left disabled; do not enable unless AWS lifts this restriction.
data_capture_enabled = false
